services:
  next-app:
    container_name: next-app
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
    volumes:
      # - ./src:/app/src
      - ./public:/app/public
    restart: always
    ports:
      - 3000:3000
